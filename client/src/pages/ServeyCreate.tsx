import React from 'react';
import styled from 'styled-components';

const ServeyCreateBlock = styled.div``;

interface ServeyCreateProps {}

const ServeyCreate: React.SFC<ServeyCreateProps> = () => {
  return <ServeyCreateBlock>Hello world</ServeyCreateBlock>;
};

export default ServeyCreate;
